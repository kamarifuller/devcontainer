
{
	"name": "Ubuntu",

	"build": {
		"dockerfile": "Dockerfile"
	},

		// Configure tool-specific properties.
		"customizations": {
			"vscode": {
				"extensions": [
					"dart-code.dart-code",
					"dart-code.flutter"
				]
			}
		}

}
